import React from "react";

class CreateGroup extends React.Component {
	render() {
		return (
			<h1>
				{" "}
				Form to create a new group. On submit, should also auto register
				the user and go back to explore groups
			</h1>
		);
	}
}

export default CreateGroup;
